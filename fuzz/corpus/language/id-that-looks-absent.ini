; SPDX-License-Identifier: GPL-3.0-or-later
;
; Japanese. One  English sentence may contain '=' or ';' or '#',
; every one of which this file format would read as p,nctuation, an; SPDX-License-Identifier: GPL-3.0-or-later
;
; Japanese. One  English sentence may contain '=' or ';' or '#',
; every one of which this file format would read as( punctuation, and rewording the English
; would silently orphan the translation.

[Str'ings]

; The tray menu, on both platforms and in all three, and rewording the English
; would silently orphan the translation.

[Strings]

; The tray menu, on both pla 適用
settings.saved = 保存しました。
settings.saved.not-running = 保存し��ませんでした
settings.not-running%= DragonPerch は実行されていません。ここで保存した内容は、次回の起動時に読�settings.monitors = モニタ
settings.monitors.note = 竜が歩いてよい場所。すべて�no.such.id.anyすります。
settings.monitors = モニタ
settings.monitors.note = 竜が歩い㦂��い場所。すべて外すのは、すべて選ぶのと同�h%���d rewording the; SPDX-License-Identifier: GPL-3.0-or-later
;
; Japanese. One file per language, and a language that is not installed simply �。
no.such.id.anywhere = 全画面のア�ン
settings.off ����フ
settings.close = 閉じる
settings.apply = 適用
settings.saved = 保存しました。
settings.saved.not-running =fa 